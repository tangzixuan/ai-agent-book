# === 实验 10-8 环境变量示例：复制为 .env 后填入你的 Key ===

# 每个玩家 Agent 都用 OpenAI Chat Completions（默认 gpt-4o-mini）。
# 可选的语音合成用 OpenAI TTS（tts-1）。二者都读这一个 Key。
OPENAI_API_KEY=your_openai_api_key_here

# 可选：覆盖模型（默认 gpt-4o-mini）
# OPENAI_MODEL=gpt-4o-mini

# 注意：本实验只用 OpenAI。
# OPENROUTER / ANTHROPIC / DEEPSEEK / SILICONFLOW 的 Key 当前不可用，请勿使用。
