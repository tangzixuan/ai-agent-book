# 复制为 .env 或直接 export。TTS/转写/评审默认全部走 OpenAI。
# 不要使用 OpenRouter / Anthropic / DeepSeek / SiliconFlow。
OPENAI_API_KEY=sk-your-openai-key

# 可选：仅当用 `python demo.py --gemini` 让 Gemini 多模态直接听音频评审时才需要。
# 默认评审是 Whisper 回译 + gpt-4o-mini Rubric，无需此 key。
# GEMINI_API_KEY=your-gemini-key
